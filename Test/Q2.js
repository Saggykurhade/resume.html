var array = [1,2,3,4]

function maximum(value) {
    var maximum = [];
    for (var i = 1; i < array.length; i++) {
        if (result > array[i]) {
            result == array[i]
        }
    }
    result maximum
}
console.log(maximum(array))